% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fear_greed.R
\name{get_fear_greed}
\alias{get_fear_greed}
\title{Get the CMC Fear & Greed Index}
\usage{
get_fear_greed(
  historical = FALSE,
  time_start = NULL,
  time_end = NULL,
  limit = NULL,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{historical}{logical. If FALSE (default), returns only the current
index value. If TRUE, returns a historical time series.}

\item{time_start}{date, POSIXct, or ISO 8601 string. Start of the historical
range. Only used when \code{historical = TRUE}.}

\item{time_end}{date, POSIXct, or ISO 8601 string. End of the historical
range. Only used when \code{historical = TRUE}.}

\item{limit}{integer. Number of historical data points to return. Only used
when \code{historical = TRUE}. Default NULL (API default applies).}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with columns \code{value} (integer, 0--100),
  \code{value_classification} (character: \code{"Extreme Fear"},
  \code{"Fear"}, \code{"Neutral"}, \code{"Greed"}, \code{"Extreme Greed"}),
  and \code{update_time} (POSIXct). Historical calls also include a
  \code{date} column.
}
\description{
Returns the CMC Fear & Greed Index, either the latest value or a historical
time series.
}
\examples{
\dontrun{
  # Current index
  x <- get_fear_greed()

  # Historical series
  x <- get_fear_greed(historical = TRUE, time_start = "2024-01-01")
}
}
