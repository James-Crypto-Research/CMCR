% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_price_conversion.R
\name{get_price_conversion}
\alias{get_price_conversion}
\title{Convert an amount from one currency to another}
\usage{
get_price_conversion(
  amount,
  symbol,
  quote_currency = "USD",
  time = NULL,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{amount}{numeric. The amount to convert.}

\item{symbol}{character. The source currency symbol, e.g. \code{"BTC"}.}

\item{quote_currency}{character. The target currency symbol. Default
\code{"USD"}.}

\item{time}{date, POSIXct, or ISO 8601 string. Historical price point to use
for conversion. If NULL (default), uses the latest price.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A single-row tibble with columns \code{id}, \code{name},
  \code{symbol}, \code{amount}, \code{last_updated}, \code{price}, and
  \code{quote_currency}.
}
\description{
Converts an amount from a source currency to a target currency using the
latest or a historical CMC price.
}
\examples{
\dontrun{
  # How many USD is 1.5 BTC worth right now?
  get_price_conversion(1.5, "BTC")

  # Historical conversion
  get_price_conversion(1, "ETH", time = "2023-01-01")
}
}
