% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_quotes_latest.R
\name{get_quotes_latest}
\alias{get_quotes_latest}
\title{Get the latest price quotes for one or more cryptocurrencies}
\usage{
get_quotes_latest(
  symbol = NULL,
  id = NULL,
  quote_currency = "USD",
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{symbol}{character vector. One or more CMC token symbols, e.g.
\code{c("BTC", "ETH")}. Either \code{symbol} or \code{id} must be
provided.}

\item{id}{integer vector. CMC cryptocurrency IDs as an alternative to
\code{symbol}. Only one of \code{symbol} or \code{id} should be provided.}

\item{quote_currency}{character. Currency for price conversion. Default
\code{"USD"}.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per requested symbol. Key columns include
  \code{id}, \code{name}, \code{symbol}, \code{slug}, \code{cmc_rank},
  \code{circulating_supply}, \code{total_supply}, \code{max_supply},
  and price/volume columns prefixed with \code{quote_USD_}.
}
\description{
Returns a point-in-time price snapshot for one or more tokens. This is the
most credit-efficient way to retrieve current prices for a watchlist.
Complements the historical \code{get_token_quote()}.
}
\examples{
\dontrun{
  # Single token
  x <- get_quotes_latest("BTC")

  # Multiple tokens
  x <- get_quotes_latest(c("BTC", "ETH", "SOL"))
}
}
