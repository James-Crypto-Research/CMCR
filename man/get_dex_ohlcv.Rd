% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_dex_ohlcv.R
\name{get_dex_ohlcv}
\alias{get_dex_ohlcv}
\title{Get historical OHLCV data for a DEX trading pair}
\usage{
get_dex_ohlcv(
  pair_address,
  network_id,
  time_start = Sys.Date() - 7,
  time_end = Sys.Date(),
  interval = "daily",
  quote_currency = "USD",
  as_date = TRUE,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{pair_address}{character. The on-chain contract address of the trading
pair (hex string). Obtain from \code{get_dex_pairs()}.}

\item{network_id}{integer. The blockchain network ID. Obtain from
\code{get_dex_networks()}.}

\item{time_start}{date, POSIXct, or ISO 8601 string. Start of the interval.
Default 7 days ago.}

\item{time_end}{date, POSIXct, or ISO 8601 string. End of the interval.
Default today.}

\item{interval}{character. Bar frequency. One of \code{"1m"}, \code{"5m"},
\code{"15m"}, \code{"30m"}, \code{"1h"}, \code{"4h"}, \code{"daily"},
\code{"weekly"}. Default \code{"daily"}.}

\item{quote_currency}{character. Currency for OHLCV values. Default
\code{"USD"}.}

\item{as_date}{logical. If TRUE and \code{interval} is \code{"daily"}, the
\code{time_open} column is returned as a \code{Date}. Default TRUE.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per OHLCV bar. Columns: \code{time_open},
  \code{pair_address}, \code{network_id}, \code{open}, \code{high},
  \code{low}, \code{close}, \code{volume}.
}
\description{
Returns historical OHLCV bars for a specific on-chain trading pair. Use
\code{get_dex_pairs()} to find valid \code{pair_address} and
\code{network_id} values.
}
\examples{
\dontrun{
  # Daily bars for a Uniswap v3 WETH/USDC pair on Ethereum
  x <- get_dex_ohlcv(
    pair_address = "0x88e6a0c2ddd26feeb64f039a2c41296fcb3f5640",
    network_id   = 1
  )
}
}
