% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_categories.R
\name{get_category}
\alias{get_category}
\title{Get coins within a cryptocurrency category}
\usage{
get_category(
  id,
  start = 1,
  limit = 100,
  quote_currency = "USD",
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{id}{character. The category ID (from \code{get_categories()}).
Required.}

\item{start}{integer. Offset for pagination within the category coin list.
Default 1.}

\item{limit}{integer. Number of coins to return (max 1000). Default 100.}

\item{quote_currency}{character. Currency for price conversion. Default
\code{"USD"}.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per coin in the category. Columns follow the
  same structure as \code{get_listings()}: \code{id}, \code{name},
  \code{symbol}, \code{cmc_rank}, market data, and
  \code{quote_USD_*} price columns.
}
\description{
Returns the list of coins that belong to a specific CoinMarketCap category.
Use \code{get_categories()} to discover available category IDs.
}
\examples{
\dontrun{
  # Find the DeFi category ID first
  cats <- get_categories()
  defi_id <- cats$id[cats$name == "DeFi"]

  # Fetch coins in that category
  x <- get_category(defi_id)
}
}
