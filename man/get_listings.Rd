% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_listings.R
\name{get_listings}
\alias{get_listings}
\title{Get the latest cryptocurrency listings}
\usage{
get_listings(
  start = 1,
  limit = 100,
  sort = "market_cap",
  sort_dir = "desc",
  cryptocurrency_type = "all",
  quote_currency = "USD",
  market_cap_min = NULL,
  market_cap_max = NULL,
  auto_paginate = FALSE,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{start}{integer. Rank offset for pagination. Default 1.}

\item{limit}{integer. Number of results per page (max 5000). Default 100.}

\item{sort}{character. Sort field. One of \code{"market_cap"}, \code{"name"},
\code{"symbol"}, \code{"price"}, \code{"circulating_supply"},
\code{"total_supply"}, \code{"24h_volume"}, \code{"percent_change_1h"},
\code{"percent_change_24h"}, \code{"percent_change_7d"}. Default
\code{"market_cap"}.}

\item{sort_dir}{character. \code{"asc"} or \code{"desc"}. Default
\code{"desc"}.}

\item{cryptocurrency_type}{character. \code{"all"}, \code{"coins"}, or
\code{"tokens"}. Default \code{"all"}.}

\item{quote_currency}{character. Currency for price conversion. Default
\code{"USD"}.}

\item{market_cap_min}{numeric. Minimum market cap filter. Default NULL.}

\item{market_cap_max}{numeric. Maximum market cap filter. Default NULL.}

\item{auto_paginate}{logical. If TRUE, fetches all pages automatically by
incrementing \code{start}. Default FALSE. Note: may consume many API
credits.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per cryptocurrency. Key columns include
  \code{id}, \code{name}, \code{symbol}, \code{cmc_rank},
  \code{circulating_supply}, \code{total_supply}, \code{max_supply},
  \code{date_added}, \code{quote_USD_price}, \code{quote_USD_volume_24h},
  \code{quote_USD_market_cap}, \code{quote_USD_percent_change_1h},
  \code{quote_USD_percent_change_24h}, \code{quote_USD_percent_change_7d}.
  The \code{tags} column is a list-column of character vectors.
}
\description{
Returns a ranked list of all active cryptocurrencies with the latest market
data. This is the primary market screener endpoint.
}
\examples{
\dontrun{
  # Top 100 by market cap
  x <- get_listings()

  # Top 500 tokens only
  x <- get_listings(limit = 500, cryptocurrency_type = "tokens")

  # Full market (all pages)
  x <- get_listings(limit = 5000, auto_paginate = TRUE)
}
}
