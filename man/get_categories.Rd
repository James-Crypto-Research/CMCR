% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_categories.R
\name{get_categories}
\alias{get_categories}
\title{Get cryptocurrency categories}
\usage{
get_categories(
  start = 1,
  limit = 100,
  symbol = NULL,
  id = NULL,
  slug = NULL,
  auto_paginate = FALSE,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{start}{integer. Offset for pagination. Default 1.}

\item{limit}{integer. Number of results per page (max 5000). Default 100.}

\item{symbol}{character. Optional. Filter to categories that contain the
given symbol(s). Accepts a character vector; multiple values are
collapsed to a comma-separated string.}

\item{id}{character. Optional. Filter to categories that contain the given
CMC coin ID(s). Accepts a character vector.}

\item{slug}{character. Optional. Filter to categories that contain the given
coin slug(s). Accepts a character vector.}

\item{auto_paginate}{logical. If TRUE, fetches all pages automatically.
Default FALSE.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per category. Key columns include \code{id},
  \code{name}, \code{title}, \code{description}, \code{num_tokens},
  \code{avg_price_change}, \code{market_cap}, \code{market_cap_change},
  \code{volume}, \code{volume_change}, \code{last_updated}.
}
\description{
Returns information about all coin categories available on CoinMarketCap.
Categories group coins by theme or sector (e.g. DeFi, Layer 1, Gaming).
Use \code{get_category()} to fetch the coins within a specific category.
}
\examples{
\dontrun{
  # All categories
  x <- get_categories()

  # Categories containing BTC
  x <- get_categories(symbol = "BTC")
}
}
