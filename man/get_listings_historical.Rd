% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_listings_historical.R
\name{get_listings_historical}
\alias{get_listings_historical}
\title{Get a historical snapshot of cryptocurrency listings}
\usage{
get_listings_historical(
  date,
  start = 1,
  limit = 100,
  sort = "cmc_rank",
  sort_dir = "desc",
  cryptocurrency_type = "all",
  quote_currency = "USD",
  auto_paginate = FALSE,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{date}{date, POSIXct, or ISO 8601 string. The historical date for the
snapshot. Required.}

\item{start}{integer. Rank offset for pagination. Default 1.}

\item{limit}{integer. Number of results per page (max 5000). Default 100.}

\item{sort}{character. Sort field. One of \code{"cmc_rank"},
\code{"name"}, \code{"symbol"}, \code{"market_cap"}, \code{"price"},
\code{"circulating_supply"}, \code{"total_supply"},
\code{"percent_change_24h"}. Default \code{"cmc_rank"}.}

\item{sort_dir}{character. \code{"asc"} or \code{"desc"}. Default
\code{"desc"}.}

\item{cryptocurrency_type}{character. \code{"all"}, \code{"coins"}, or
\code{"tokens"}. Default \code{"all"}.}

\item{quote_currency}{character. Currency for price conversion. Default
\code{"USD"}.}

\item{auto_paginate}{logical. If TRUE, fetches all pages automatically.
Default FALSE.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per cryptocurrency at the requested date.
  Columns mirror \code{get_listings()}: \code{id}, \code{name},
  \code{symbol}, \code{cmc_rank}, supply columns, and
  \code{quote_USD_*} price columns.
}
\description{
Returns a ranked snapshot of all active cryptocurrencies and their market
data at a specific point in time. Useful for reconstructing historical
rankings and market-cap league tables.
}
\details{
Requires a Hobbyist or higher CMC Pro plan.
}
\examples{
\dontrun{
  # Top 100 by rank on 1 Jan 2022
  x <- get_listings_historical("2022-01-01")

  # All coins on a date (auto-paginate)
  x <- get_listings_historical("2021-01-01", limit = 5000,
                                auto_paginate = TRUE)
}
}
