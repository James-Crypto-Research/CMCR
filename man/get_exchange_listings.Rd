% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_exchange_listings.R
\name{get_exchange_listings}
\alias{get_exchange_listings}
\title{Get the latest exchange listings}
\usage{
get_exchange_listings(
  start = 1,
  limit = 100,
  sort = "volume_24h",
  sort_dir = "desc",
  market_type = "all",
  quote_currency = "USD",
  auto_paginate = FALSE,
  api_key = Sys.getenv("CMC_API_KEY")
)
}
\arguments{
\item{start}{integer. Pagination offset. Default 1.}

\item{limit}{integer. Results per page (max 5000). Default 100.}

\item{sort}{character. Sort field. One of \code{"volume_24h"},
\code{"num_market_pairs"}, \code{"exchange_score"}. Default
\code{"volume_24h"}.}

\item{sort_dir}{character. \code{"asc"} or \code{"desc"}. Default
\code{"desc"}.}

\item{market_type}{character. Exchange type filter. One of \code{"all"},
\code{"fees"}, \code{"no_fees"}, \code{"percent"},
\code{"transactional_mining"}. Default \code{"all"}.}

\item{quote_currency}{character. Currency for volume values. Default
\code{"USD"}.}

\item{auto_paginate}{logical. If TRUE, fetches all pages automatically.
Default FALSE.}

\item{api_key}{character. CMC Pro API key. Defaults to the
\code{CMC_API_KEY} environment variable.}
}
\value{
A tibble with one row per exchange. Key columns include \code{id},
  \code{name}, \code{slug}, \code{num_market_pairs}, \code{visits},
  \code{volume_24h}, \code{volume_7d}, \code{volume_30d}, and
  \code{last_updated}. The \code{fiats} column is a list-column.
}
\description{
Returns a ranked list of all active exchanges with aggregate volume and
market pair counts. The exchange-side equivalent of \code{get_listings()}.
}
\examples{
\dontrun{
  # Top 100 exchanges by volume
  x <- get_exchange_listings()
}
}
